{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>gabe565/renovate-config"
  ],
  "regexManagers": [
    {
      "fileMatch": ["^\\.github/workflows/", "README.md"],
      "matchStrings": [
        "\\s*RESTIC_IMAGE: (?<depName>.+)\\n\\s*RESTIC_TAG: (?<currentValue>.+)\\n\\s*RESTIC_DIGEST: (?<currentDigest>.+)",
        "<!--renovate image=(?<depName>.+) -->\\n.*\\/Version-v(?<currentValue>.+?)-"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "matchPackageNames": ["ghcr.io/restic/restic"],
      "automerge": true
    }
  ]
}
